# Copy to .env. Never commit .env.
# CURSEFORGE_API_KEY is the development path only: a release build carries its own key,
# compiled in from GCL_CURSEFORGE_API_KEY. config.toml never holds either one.
CURSEFORGE_API_KEY=
GCL_MSA_CLIENT_ID=
